import React from 'react';
import {storiesOf} from '@storybook/react';

import {MoreCardsIcon} from './MoreCardsIcon';

storiesOf(`Icons|MoreCardsIcon`, module)
    .add(`default`, () => <MoreCardsIcon />);
